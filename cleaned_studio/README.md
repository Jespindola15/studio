Minimal Next.js static site ready for GitHub Pages.

How to use:

1. cd cleaned_studio
2. npm ci
3. npm run build
4. git init && git add . && git commit -m "initial"
5. Create a repo on GitHub and push this folder as the repository root

If you want me to instead delete files in the original repo, tell me and I'll proceed (I kept a backup in this workspace).
